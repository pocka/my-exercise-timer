[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = tab

[*.md]
# Trailing whitespace is used as hard linebreak in Markdown
trim_trailing_whitespace = false
